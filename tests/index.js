const tape = require('tape')
const ipfs = require('ipfs')
const graph = require('ipld-graph-builder')
const Hypervisor = require('../')

tape('base kernel tests', t => {
  const state = {}
  const hypervisor = new Hypervisor()
  t.equal()
})
